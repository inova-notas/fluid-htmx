{% raw %}
<section id="components" class="py-24 bg-base-100">
    <div class="max-w-5xl mx-auto px-6">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold">Behavioral Components</h2>
            <p class="mt-4 text-lg text-base-content/60">HTMX-powered interactions &mdash; not just CSS wrappers. Eject and customize any of them.</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

            <!-- Toast -->
            <div class="card bg-base-200 shadow-sm">
                <div class="card-body">
                    <h3 class="card-title">Toast</h3>
                    <p class="text-base-content/70 text-sm">Auto-dismissing notification. Uses <code class="text-primary text-xs">hx-on::load</code> to self-remove after a duration.</p>
                    <div class="code-block mt-4">
<pre><code><span class="fn">{%</span> <span class="kw">render</span> <span class="str">'components/toast'</span>,
    message: <span class="str">'Saved!'</span>,
    type: <span class="str">'success'</span>,
    duration: <span class="str">3000</span> <span class="fn">%}</span></code></pre>
                    </div>
                    <div class="mt-3 flex flex-wrap gap-1">
                        <span class="badge badge-sm badge-ghost">message</span>
                        <span class="badge badge-sm badge-ghost">type</span>
                        <span class="badge badge-sm badge-ghost">dismissible</span>
                        <span class="badge badge-sm badge-ghost">duration</span>
                    </div>
                </div>
            </div>

            <!-- Confirm Dialog -->
            <div class="card bg-base-200 shadow-sm">
                <div class="card-body">
                    <h3 class="card-title">Confirm Dialog</h3>
                    <p class="text-base-content/70 text-sm">Native <code class="text-primary text-xs">&lt;dialog&gt;</code> with HTMX confirm action. No server round-trip to show.</p>
                    <div class="code-block mt-4">
<pre><code><span class="fn">{%</span> <span class="kw">render</span> <span class="str">'components/confirm-dialog'</span>,
    id: <span class="str">'delete-user'</span>,
    title: <span class="str">'Delete user?'</span>,
    confirm_url: <span class="str">'/users/1'</span>,
    confirm_method: <span class="str">'delete'</span> <span class="fn">%}</span></code></pre>
                    </div>
                    <div class="mt-3 flex flex-wrap gap-1">
                        <span class="badge badge-sm badge-ghost">id</span>
                        <span class="badge badge-sm badge-ghost">title</span>
                        <span class="badge badge-sm badge-ghost">message</span>
                        <span class="badge badge-sm badge-ghost">confirm_url</span>
                        <span class="badge badge-sm badge-ghost">confirm_method</span>
                    </div>
                </div>
            </div>

            <!-- Modal -->
            <div class="card bg-base-200 shadow-sm">
                <div class="card-body">
                    <h3 class="card-title">Modal</h3>
                    <p class="text-base-content/70 text-sm">HTMX-loaded modal &mdash; fetches content from the server when opened via <code class="text-primary text-xs">hx-get</code>.</p>
                    <div class="code-block mt-4">
<pre><code><span class="fn">{%</span> <span class="kw">render</span> <span class="str">'components/modal'</span>,
    id: <span class="str">'edit-user'</span>,
    title: <span class="str">'Edit User'</span>,
    url: <span class="str">'/users/1/edit'</span>,
    size: <span class="str">'lg'</span> <span class="fn">%}</span></code></pre>
                    </div>
                    <div class="mt-3 flex flex-wrap gap-1">
                        <span class="badge badge-sm badge-ghost">id</span>
                        <span class="badge badge-sm badge-ghost">title</span>
                        <span class="badge badge-sm badge-ghost">url</span>
                        <span class="badge badge-sm badge-ghost">trigger_label</span>
                        <span class="badge badge-sm badge-ghost">size</span>
                    </div>
                </div>
            </div>

        </div>

        <!-- Eject -->
        <div class="mt-12 text-center">
            <div class="card bg-base-200 shadow-sm max-w-2xl mx-auto">
                <div class="card-body">
                    <h3 class="card-title justify-center">Eject &amp; Customize</h3>
                    <p class="text-base-content/70 text-sm">Components are embedded in the DLL. Eject them to your project to edit freely &mdash; local files always win.</p>
                    <div class="code-block mt-4 text-left">
<pre><code>fluid.<span class="fn">EjectAllComponents</span>();      <span class="cmt">// all at once</span>
fluid.<span class="fn">EjectComponent</span>(<span class="str">"toast"</span>);   <span class="cmt">// or one by one</span></code></pre>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endraw %}

<div class="container mx-auto px-4 py-12">
    <h1 class="text-4xl font-bold text-center mb-8">About This Sample</h1>

    <div class="stats shadow w-full mb-8">
        <div class="stat">
            <div class="stat-title">Template Engine</div>
            <div class="stat-value text-primary">Fluid</div>
            <div class="stat-desc">Liquid-compatible for .NET</div>
        </div>
        <div class="stat">
            <div class="stat-title">CSS Framework</div>
            <div class="stat-value text-secondary">DaisyUI</div>
            <div class="stat-desc">Build-time compiled</div>
        </div>
        <div class="stat">
            <div class="stat-title">Interactivity</div>
            <div class="stat-value text-accent">HTMX</div>
            <div class="stat-desc">Hypermedia-driven</div>
        </div>
    </div>

    <div class="divider">How It Works</div>

    <ul class="steps steps-vertical lg:steps-horizontal w-full">
        <li class="step step-primary">Install TailwindCssTool</li>
        <li class="step step-primary">Download DaisyUI bundles</li>
        <li class="step step-primary">Build CSS at compile time</li>
        <li class="step step-primary">Serve with ASP.NET Core</li>
    </ul>

    <div class="divider"></div>

    <div class="card bg-base-100 shadow-xl">
        <div class="card-body">
            <h2 class="card-title">No Node.js Required</h2>
            <p>
                This sample uses the standalone Tailwind CSS CLI via
                <code class="badge badge-outline">AustinS.TailwindCssTool</code>
                and DaisyUI's standalone <code class="badge badge-outline">.mjs</code> bundles.
                Everything is downloaded and compiled at build time through MSBuild targets.
            </p>
        </div>
    </div>
</div>

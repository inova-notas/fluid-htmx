<div class="breadcrumbs text-sm">
  <ul>
    {% for item in items %}
    <li>
      {% if item.url %}
        <a href="{{ item.url }}">{{ item.label }}</a>
      {% else %}
        {{ item.label }}
      {% endif %}
    </li>
    {% endfor %}
  </ul>
</div>

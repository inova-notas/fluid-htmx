<div class="py-4 space-y-4">
    <p>This content was loaded from the server via <strong>HTMX</strong>.</p>
    <p class="text-base-content/70">
        The modal component uses <code>hx-get</code> with <code>hx-trigger="intersect once"</code>
        to fetch content when the dialog becomes visible.
    </p>
    <div class="stats shadow w-full">
        <div class="stat">
            <div class="stat-title">Loaded at</div>
            <div class="stat-value text-sm">Just now</div>
            <div class="stat-desc">Content fetched via HTMX</div>
        </div>
    </div>
</div>
